name: Build and Deploy Back
description: Build and deploy express app to Cloud Run.

runs:
  using: composite
  steps:
    - name: 🔨 Get Variables
      id: variables
      shell: bash
      run: echo "::set-output name=image::gcr.io/$PROJECT_ID/$SERVICE_NAME"

    - id: auth
      name: 🗝️ Authenticate to Google Cloud
      uses: google-github-actions/auth@v0
      with:
        create_credentials_file: true
        token_format: access_token
        workload_identity_provider: ${{ env.IDENTITY_PROVIDER }}
        service_account: ${{ env.SERVICE_ACCOUNT_EMAIL }}

    - name: 🐳 Setup docker
      uses: docker/login-action@v1
      with:
        registry: gcr.io
        username: oauth2accesstoken
        password: ${{ steps.auth.outputs.access_token }}

    - name: 📦 Build image
      shell: bash
      working-directory: src
      run: |-
        docker build \
          -t "${{ steps.variables.outputs.image }}:$GITHUB_SHA" \
          -t "${{ steps.variables.outputs.image }}:latest" \
          -f ./back/Dockerfile \
          .

    - name: 📤 Publish image
      shell: bash
      working-directory: src
      run: |-
        docker push "${{ steps.variables.outputs.image }}" -a

    - name: 🚀 Deploy to Cloud Run
      uses: google-github-actions/deploy-cloudrun@v0
      with:
        service: ${{ env.SERVICE_NAME }}
        image: ${{ steps.variables.outputs.image }}:latest
        region: us-central1
        flags: |
          --service-account=cloud-run-sa@${{ env.PROJECT_ID }}.iam.gserviceaccount.com
          --max-instances=10
          --min-instances=${{ env.MIN_INSTANCES }}
          --set-cloudsql-instances=app
          --allow-unauthenticated
        env_vars: |
          ADMIN_EMAIL=${{ env.ADMIN_EMAIL }}
          API=${{ env.API }}
          DATABASE_HOST=${{ env.DATABASE_HOST }}
          ENVIRONMENT=${{ env.ENVIRONMENT }}
          PROJECT_ID=${{ env.PROJECT_ID }}
          WEBSITE=${{ env.WEBSITE }}
        secrets: |
          /secrets/client-id=client-id:latest
          /secrets/database-password=database-password:latest
          /secrets/sendgrid-api-key=sendgrid-api-key:latest
